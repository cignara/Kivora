cat > /mnt/user-data/outputs/kivora-creative-kingdom-v3.html << 'HTMLEOF'
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Kivora · Creative Kingdom · Colour the Magic Butterfly</title>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --purple:#7C3AED;--purple-l:#EDE9FE;--purple-d:#5B21B6;
  --gold:#F59E0B;--gold-l:#FEF9C3;
  --green:#059669;--green-l:#ECFDF5;
  --blue:#3B82F6;--blue-l:#EFF6FF;
  --red-l:#FEF2F2;
  --border:#E5E7EB;--border-md:#D1D5DB;
  --text:#111827;--muted:#6B7280;
  --bg:#F9FAFB;--white:#fff;
  --r-sm:8px;--r-md:12px;--r-lg:16px;--r-xl:24px;
  --shadow:0 2px 12px rgba(0,0,0,.08);--shadow-lg:0 8px 32px rgba(0,0,0,.16);
}
body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:var(--bg);color:var(--text);min-height:100vh;display:flex;flex-direction:column;align-items:center;padding:20px 16px 60px}

/* HEADER */
.hdr{width:100%;max-width:860px;display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;gap:12px}
.hdr-left{}
.badge{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;letter-spacing:.6px;color:var(--purple);background:var(--purple-l);padding:3px 10px;border-radius:20px;margin-bottom:6px}
.hdr h1{font-size:22px;font-weight:800;color:var(--text);line-height:1.2}
.hdr p{font-size:13px;color:var(--muted);margin-top:3px}
.hdr-btns{display:flex;gap:8px;flex-shrink:0}
.hdr-btn{display:flex;align-items:center;gap:7px;padding:9px 15px;background:var(--white);border:1.5px solid var(--border);border-radius:var(--r-md);font-size:13px;font-weight:600;color:var(--text);cursor:pointer;transition:all .15s;box-shadow:var(--shadow);white-space:nowrap}
.hdr-btn:hover{border-color:var(--purple);color:var(--purple)}
.count-badge{background:var(--purple);color:#fff;font-size:11px;font-weight:700;padding:1px 7px;border-radius:20px;display:none}
.count-badge.show{display:inline-block}

/* GAME CARD */
.game-card{width:100%;max-width:860px;background:var(--white);border:1.5px solid var(--border);border-radius:var(--r-xl);overflow:hidden;box-shadow:var(--shadow-lg)}

/* TOP BAR */
.top-bar{display:flex;align-items:center;justify-content:space-between;padding:11px 18px;border-bottom:1px solid var(--border)}
.tbl{font-size:13px;font-weight:600;color:var(--text)}
.tbm{font-size:12px;color:var(--muted);font-weight:400}
.reset-btn{display:flex;align-items:center;gap:5px;padding:6px 12px;border:1px solid var(--border);border-radius:var(--r-sm);background:none;font-size:12px;font-weight:600;color:var(--muted);cursor:pointer;transition:all .15s}
.reset-btn:hover{border-color:#9CA3AF;color:var(--text)}

/* MAIN */
.main-area{display:flex}

/* CANVAS */
.canvas-wrap{flex:1;border-right:1.5px solid var(--border);position:relative;background:var(--white);overflow:hidden}
.canvas-wrap svg{width:100%;display:block}

/* PALETTE */
.palette-panel{width:172px;flex-shrink:0;background:#FAFAFA;padding:14px 11px;overflow-y:auto}
.pal-heading{font-size:10px;font-weight:800;letter-spacing:1px;text-transform:uppercase;color:var(--muted);margin-bottom:10px;text-align:center}
.pal-item{width:100%;border-radius:var(--r-md);margin-bottom:7px;border:2.5px solid transparent;overflow:hidden;cursor:pointer;transition:transform .15s,border-color .12s,box-shadow .15s;background:var(--white);box-shadow:0 1px 4px rgba(0,0,0,.08)}
.pal-item:hover:not(.pdone){transform:translateY(-2px);box-shadow:0 4px 12px rgba(0,0,0,.14)}
.pal-item.pactive{border-color:#222;transform:scale(1.05);box-shadow:0 6px 20px rgba(0,0,0,.18)}
.pal-item.pdone{cursor:default;opacity:.65}
.pal-item.wobble{animation:wobble .55s ease}
.pal-color{height:42px;display:flex;align-items:center;justify-content:center;font-size:20px;font-weight:900;color:rgba(0,0,0,.4);position:relative;transition:filter .2s}
.pal-info{padding:5px 8px;background:var(--white);display:flex;align-items:center;justify-content:space-between;border-top:1px solid rgba(0,0,0,.06)}
.pal-name{font-size:11px;font-weight:700;color:var(--text)}
.pal-count{font-size:10px;font-weight:600;color:var(--muted)}
.pal-count.done{color:var(--green)}

/* BOTTOM BAR */
.bot-bar{padding:13px 18px;border-top:1px solid var(--border);background:var(--white)}
.prog-row{display:flex;justify-content:space-between;align-items:center;margin-bottom:7px}
.prog-lbl{font-size:12px;color:var(--muted)}
.prog-pct{font-size:13px;font-weight:700;color:var(--text)}
.prog-track{height:8px;background:var(--border);border-radius:4px;overflow:hidden;margin-bottom:11px}
.prog-fill{height:100%;width:0%;background:linear-gradient(90deg,var(--purple),var(--gold));border-radius:4px;transition:width .4s cubic-bezier(.4,0,.2,1)}
.hint{display:flex;align-items:center;gap:8px;padding:9px 13px;border-radius:var(--r-sm);font-size:13px;line-height:1.4}
.hint.idle{background:var(--bg);color:var(--muted);border:1px solid var(--border)}
.hint.active{background:var(--purple-l);color:var(--purple-d);border:1px solid #C4B5FD;font-weight:600}
.hint.complete{background:var(--green-l);color:#065F46;border:1px solid #A7F3D0;font-weight:600}
.hint-dot{width:12px;height:12px;border-radius:3px;flex-shrink:0;border:1px solid rgba(0,0,0,.1)}

/* META */
.meta-strip{width:100%;max-width:860px;margin-top:14px;padding:12px 16px;background:var(--white);border:1.5px solid var(--border);border-radius:var(--r-lg);display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:10px;box-shadow:var(--shadow)}
.meta-lbl{font-size:10px;color:var(--muted);margin-bottom:2px;text-transform:uppercase;letter-spacing:.5px;font-weight:600}
.meta-val{font-size:13px;font-weight:700;color:var(--text)}

/* CONFETTI */
#confetti-layer{position:fixed;top:0;left:0;right:0;height:100vh;pointer-events:none;z-index:200;overflow:hidden;display:none}
.cp{position:absolute;top:-20px;animation:cf-fall linear forwards}
@keyframes cf-fall{0%{transform:translateY(0) rotate(0deg) translateX(0);opacity:1}80%{opacity:1}100%{transform:translateY(108vh) rotate(720deg) translateX(var(--drift,50px));opacity:0}}

/* TOAST */
#toast{position:fixed;bottom:30px;left:50%;transform:translateX(-50%) translateY(20px);background:#1F2937;color:#fff;padding:12px 24px;border-radius:30px;font-size:14px;font-weight:600;z-index:800;opacity:0;pointer-events:none;transition:transform .3s,opacity .3s;white-space:nowrap}
#toast.show{transform:translateX(-50%) translateY(0);opacity:1}

/* COMPLETION OVERLAY */
#comp-overlay{display:none;position:fixed;inset:0;z-index:300;background:rgba(0,0,0,.55);align-items:center;justify-content:center}
#comp-overlay.show{display:flex}
.comp-card{background:var(--white);border-radius:28px;padding:44px 40px 36px;text-align:center;max-width:420px;width:92%;box-shadow:0 24px 64px rgba(0,0,0,.3);animation:card-pop .5s cubic-bezier(.34,1.56,.64,1) both}

/* Stars */
.stars-row{display:flex;align-items:center;justify-content:center;gap:6px;margin-bottom:16px}
.star{line-height:1;color:var(--gold);animation:star-pop .5s cubic-bezier(.34,1.56,.64,1) both}
.star:nth-child(1){font-size:40px;animation-delay:.1s}
.star:nth-child(2){font-size:56px;animation-delay:.38s}
.star:nth-child(3){font-size:40px;animation-delay:.65s}
@keyframes star-pop{0%{transform:scale(0) rotate(-30deg);opacity:0}60%{transform:scale(1.4) rotate(12deg)}100%{transform:scale(1) rotate(0);opacity:1}}

.comp-title{font-size:28px;font-weight:900;color:var(--text);margin-bottom:6px;animation:fade-up .5s ease .85s both}
.comp-sub{font-size:15px;color:var(--muted);line-height:1.6;margin-bottom:28px;animation:fade-up .5s ease .95s both}
@keyframes fade-up{from{transform:translateY(14px);opacity:0}to{transform:translateY(0);opacity:1}}
@keyframes card-pop{from{transform:scale(.5);opacity:0}to{transform:scale(1);opacity:1}}

.comp-actions{display:flex;flex-direction:column;gap:10px;animation:fade-up .5s ease 1.05s both}
.cbtn{display:flex;align-items:center;justify-content:center;gap:9px;padding:13px 20px;border-radius:var(--r-md);font-size:14px;font-weight:700;cursor:pointer;border:2px solid transparent;transition:transform .12s,box-shadow .12s;width:100%}
.cbtn:active{transform:scale(.97)}
.cbtn.c-gallery{background:var(--purple);color:#fff;box-shadow:0 4px 14px rgba(124,58,237,.38)}
.cbtn.c-gallery:hover{box-shadow:0 6px 20px rgba(124,58,237,.5)}
.cbtn.c-dl{background:var(--gold-l);color:#78350F;border-color:#FCD34D}
.cbtn.c-dl:hover{background:#FEF3C7}
.cbtn.c-print{background:var(--blue-l);color:#1E3A8A;border-color:#BFDBFE}
.cbtn.c-print:hover{background:#DBEAFE}
.cbtn.c-reset{background:none;color:var(--muted);border-color:var(--border);font-size:13px;padding:10px 20px}
.cbtn.c-reset:hover{border-color:#9CA3AF;color:var(--text)}

/* GALLERY MODAL */
#gal-modal{display:none;position:fixed;inset:0;z-index:500;background:rgba(0,0,0,.6);align-items:flex-start;justify-content:center;padding:40px 16px;overflow-y:auto}
#gal-modal.show{display:flex}
.gal-card{background:var(--white);border-radius:24px;padding:28px;width:100%;max-width:760px;box-shadow:var(--shadow-lg);animation:card-pop .35s cubic-bezier(.34,1.56,.64,1) both}
.gal-hdr{display:flex;align-items:center;justify-content:space-between;margin-bottom:20px}
.gal-title{font-size:20px;font-weight:800}
.close-btn{width:36px;height:36px;border-radius:50%;border:1.5px solid var(--border);background:none;font-size:17px;cursor:pointer;display:flex;align-items:center;justify-content:center;color:var(--muted);transition:all .15s}
.close-btn:hover{background:var(--bg);color:var(--text)}
.gal-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:14px}
.gal-item{border:1.5px solid var(--border);border-radius:var(--r-md);overflow:hidden;background:var(--bg);transition:box-shadow .15s;cursor:pointer}
.gal-item:hover{box-shadow:0 4px 16px rgba(0,0,0,.12)}
.gal-thumb{width:100%;aspect-ratio:1;background:#fff;overflow:hidden;display:flex;align-items:center;justify-content:center}
.gal-thumb svg{width:100%;height:100%}
.gal-meta{padding:8px 10px;display:flex;align-items:center;justify-content:space-between;background:#fff;border-top:1px solid var(--border)}
.gal-date{font-size:11px;color:var(--muted);font-weight:600}
.gal-acts{display:flex;gap:4px}
.gal-act{width:28px;height:28px;border-radius:6px;border:1px solid var(--border);background:none;cursor:pointer;display:flex;align-items:center;justify-content:center;color:var(--muted);transition:all .12s}
.gal-act:hover{background:var(--bg);color:var(--text)}
.gal-act.del:hover{background:var(--red-l);color:#DC2626;border-color:#FECACA}
.gal-empty{grid-column:1/-1;text-align:center;padding:48px 20px;color:var(--muted)}
.gal-empty-icon{font-size:48px;margin-bottom:12px}
.gal-empty-t{font-size:15px;font-weight:600;color:var(--text);margin-bottom:6px}
.gal-empty-s{font-size:13px}

/* VIEWER */
#viewer{display:none;position:fixed;inset:0;z-index:600;background:rgba(0,0,0,.88);align-items:center;justify-content:center;flex-direction:column;gap:16px;padding:30px}
#viewer.show{display:flex}
#viewer-box{max-width:520px;max-height:520px;width:100%;background:#fff;border-radius:20px;padding:12px;overflow:hidden;display:flex;align-items:center;justify-content:center}
#viewer-box svg{width:100%;height:100%}
.viewer-acts{display:flex;gap:12px;flex-wrap:wrap;justify-content:center}
.vbtn{display:flex;align-items:center;gap:7px;padding:10px 22px;border-radius:30px;font-size:14px;font-weight:700;cursor:pointer;border:none;transition:all .15s}
.vbtn:hover{transform:translateY(-2px)}
.vbtn.dl{background:var(--gold);color:#fff}
.vbtn.pr{background:var(--blue);color:#fff}
.vbtn.cl{background:rgba(255,255,255,.15);color:#fff;border:1px solid rgba(255,255,255,.3)}

/* ANIMATIONS */
@keyframes wobble{0%,100%{transform:scale(1)}25%{transform:scale(1.14) rotate(-4deg)}50%{transform:scale(1.1) rotate(4deg)}75%{transform:scale(1.06) rotate(-2deg)}}
@keyframes hl{0%,100%{opacity:1}50%{opacity:.48}}.hl-r{animation:hl 1s ease infinite;cursor:pointer}

/* PRINT */
@media print{
  body{background:#fff;padding:0}
  .hdr,.meta-strip,.top-bar,.bot-bar,.palette-panel,
  #confetti-layer,#comp-overlay,#gal-modal,#viewer,#toast{display:none!important}
  .game-card{box-shadow:none;border:none;border-radius:0;max-width:100%}
  .canvas-wrap{border-right:none}
  #print-hdr,#print-ftr{display:block!important}
}
#print-hdr,#print-ftr{display:none}
#print-hdr{padding:10px 20px 8px;border-bottom:2px solid #E5E7EB;margin-bottom:6px;display:flex;align-items:center;justify-content:space-between}

/* RESPONSIVE */
@media(max-width:620px){
  .hdr{flex-direction:column;align-items:flex-start}
  .main-area{flex-direction:column}
  .canvas-wrap{border-right:none;border-bottom:1.5px solid var(--border)}
  .palette-panel{width:100%;display:grid;grid-template-columns:repeat(4,1fr);gap:6px;padding:10px}
  .pal-heading{grid-column:1/-1}
  .pal-item{margin-bottom:0}
}
</style>
</head>
<body>

<!-- PRINT HEADER (print only) -->
<div id="print-hdr">
  <div><strong style="font-size:17px">Kivora · Creative Kingdom</strong><div style="font-size:12px;color:#666;margin-top:2px">Colour the magic butterfly · Ages 4–8 · Beginner</div></div>
  <div style="font-size:11px;color:#888">kivora.lk · Educational Colouring Activity</div>
</div>

<!-- HEADER -->
<header class="hdr">
  <div class="hdr-left">
    <div class="badge">★ Creative Kingdom</div>
    <h1>Colour the magic butterfly</h1>
    <p>Pick a number · tap each matching area · complete the picture</p>
  </div>
  <div class="hdr-btns">
    <button class="hdr-btn" id="galleryBtn">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
      My Gallery
      <span class="count-badge" id="galleryCount">0</span>
    </button>
  </div>
</header>

<!-- GAME CARD -->
<main class="game-card">

  <div class="top-bar">
    <div><span class="tbl">Color-by-Number</span><span class="tbm"> · Activity 1 · Ages 4–8 · Beginner</span></div>
    <button class="reset-btn" id="resetBtn">
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-4.5"/></svg>
      Start over
    </button>
  </div>

  <div class="main-area">

    <!-- SVG CANVAS -->
    <div class="canvas-wrap">
      <svg id="canvas" viewBox="0 0 480 480" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="hlPat" x="0" y="0" width="10" height="10" patternUnits="userSpaceOnUse">
            <rect x="0" y="0" width="5" height="5" fill="#BDD0FF"/>
            <rect x="5" y="5" width="5" height="5" fill="#BDD0FF"/>
            <rect x="5" y="0" width="5" height="5" fill="#E4EBFF"/>
            <rect x="0" y="5" width="5" height="5" fill="#E4EBFF"/>
          </pattern>
        </defs>
        <!-- REGIONS — z-order bottom to top -->
        <rect   data-rid="sky"     data-num="1" x="0" y="0" width="480" height="480" fill="#EFEFEF" stroke="none"/>
        <circle data-rid="sun"     data-num="2" cx="400" cy="76" r="48"  fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="cloud-l" data-num="3" d="M 40,130 C 40,105 55,90 75,92 C 80,75 100,68 118,78 C 130,65 150,68 158,82 C 175,78 188,90 185,108 C 188,128 170,140 152,138 C 145,150 125,155 110,146 C 90,156 60,148 40,130 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="cloud-r" data-num="3" d="M 255,95 C 255,75 267,62 283,65 C 287,52 303,46 317,54 C 327,44 343,46 349,57 C 363,53 373,63 371,78 C 373,94 358,104 343,102 C 337,112 321,116 309,108 C 293,117 267,110 255,95 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="grass"   data-num="7" d="M 0,415 Q 60,400 120,410 Q 180,420 240,408 Q 300,396 360,410 Q 420,420 480,408 L 480,480 L 0,480 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="f1p"     data-num="8" d="M 100,367 L 105,379 L 117,379 L 108,387 L 111,400 L 100,393 L 89,400 L 92,387 L 83,379 L 95,379 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="f2p"     data-num="8" d="M 240,362 L 245,374 L 257,374 L 248,382 L 251,395 L 240,388 L 229,395 L 232,382 L 223,374 L 235,374 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="f3p"     data-num="8" d="M 378,364 L 383,376 L 395,376 L 386,384 L 389,397 L 378,390 L 367,397 L 370,384 L 361,376 L 373,376 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <!-- Flower centres AFTER petals — z-order fix -->
        <circle data-rid="fc1"     data-num="2" cx="100" cy="386" r="10" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <circle data-rid="fc2"     data-num="2" cx="240" cy="381" r="10" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <circle data-rid="fc3"     data-num="2" cx="378" cy="383" r="10" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="wing-ll" data-num="5" d="M 233,262 C 190,282 148,315 132,365 C 122,392 148,408 172,400 C 202,390 228,365 233,310 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="wing-lr" data-num="5" d="M 247,262 C 290,282 332,315 348,365 C 358,392 332,408 308,400 C 278,390 252,365 247,310 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="wing-ul" data-num="4" d="M 233,218 C 195,168 135,108 75,132 C 35,150 45,210 80,238 C 112,265 180,285 233,262 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <path   data-rid="wing-ur" data-num="4" d="M 247,218 C 285,168 345,108 405,132 C 445,150 435,210 400,238 C 368,265 300,285 247,262 Z" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <ellipse data-rid="body"   data-num="6" cx="240" cy="272" rx="16" ry="62" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>
        <circle  data-rid="head"   data-num="6" cx="240" cy="193" r="24" fill="#EFEFEF" stroke="#BBBBBB" stroke-width="1.5"/>

        <!-- DECORATIVE (non-interactive) -->
        <g id="decor" pointer-events="none">
          <g id="sun-rays"></g>
          <path d="M 234,170 C 222,153 210,138 198,122" stroke="#5A1A9A" stroke-width="2.5" fill="none" stroke-linecap="round"/>
          <circle cx="196" cy="119" r="5.5" fill="#5A1A9A"/>
          <path d="M 246,170 C 258,153 270,138 282,122" stroke="#5A1A9A" stroke-width="2.5" fill="none" stroke-linecap="round"/>
          <circle cx="284" cy="119" r="5.5" fill="#5A1A9A"/>
          <circle cx="233" cy="191" r="5" fill="#1A0050"/>
          <circle cx="247" cy="191" r="5" fill="#1A0050"/>
          <circle cx="235" cy="189" r="2" fill="white"/>
          <circle cx="249" cy="189" r="2" fill="white"/>
          <circle cx="150" cy="198" r="12" fill="none" stroke="#CCC" stroke-width="1.5"/>
          <circle cx="330" cy="198" r="12" fill="none" stroke="#CCC" stroke-width="1.5"/>
          <circle cx="170" cy="163" r="7"  fill="none" stroke="#CCC" stroke-width="1.5"/>
          <circle cx="310" cy="163" r="7"  fill="none" stroke="#CCC" stroke-width="1.5"/>
          <circle cx="130" cy="225" r="5"  fill="none" stroke="#CCC" stroke-width="1.5"/>
          <circle cx="350" cy="225" r="5"  fill="none" stroke="#CCC" stroke-width="1.5"/>
          <line x1="100" y1="412" x2="100" y2="386" stroke="#2E7D32" stroke-width="2.5" stroke-linecap="round"/>
          <line x1="240" y1="410" x2="240" y2="381" stroke="#2E7D32" stroke-width="2.5" stroke-linecap="round"/>
          <line x1="378" y1="411" x2="378" y2="383" stroke="#2E7D32" stroke-width="2.5" stroke-linecap="round"/>
          <ellipse cx="93"  cy="401" rx="7" ry="3" transform="rotate(-35,93,401)"  fill="none" stroke="#2E7D32" stroke-width="1.5"/>
          <ellipse cx="247" cy="396" rx="7" ry="3" transform="rotate(35,247,396)"  fill="none" stroke="#2E7D32" stroke-width="1.5"/>
          <ellipse cx="371" cy="399" rx="7" ry="3" transform="rotate(-35,371,399)" fill="none" stroke="#2E7D32" stroke-width="1.5"/>
          <path d="M35,414 L39,406 L43,414"    stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M75,412 L79,404 L83,412"    stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M130,414 L134,406 L138,414" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M185,412 L189,404 L193,412" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M300,412 L304,404 L308,412" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M350,414 L354,406 L358,414" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M405,412 L409,404 L413,412" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
          <path d="M450,414 L454,406 L458,414" stroke="#2E7D32" stroke-width="1.5" fill="none" stroke-linejoin="round"/>
        </g>

        <!-- NUMBER LABELS -->
        <g id="labels" pointer-events="none" font-family="Arial,Helvetica,sans-serif" font-weight="800" text-anchor="middle" dominant-baseline="middle" font-size="12" fill="#555">
          <text id="lbl-sky"     x="22"  y="22">1</text>
          <text id="lbl-sun"     x="400" y="79">2</text>
          <text id="lbl-fc1"     x="100" y="389">2</text>
          <text id="lbl-fc2"     x="240" y="384">2</text>
          <text id="lbl-fc3"     x="378" y="386">2</text>
          <text id="lbl-cloud-l" x="115" y="113">3</text>
          <text id="lbl-cloud-r" x="312" y="82">3</text>
          <text id="lbl-grass"   x="26"  y="455">7</text>
          <text id="lbl-f1p"     x="84"  y="380">8</text>
          <text id="lbl-f2p"     x="224" y="375">8</text>
          <text id="lbl-f3p"     x="362" y="377">8</text>
          <text id="lbl-wing-ll" x="163" y="340">5</text>
          <text id="lbl-wing-lr" x="317" y="340">5</text>
          <text id="lbl-wing-ul" x="143" y="200">4</text>
          <text id="lbl-wing-ur" x="337" y="200">4</text>
          <text id="lbl-body"    x="240" y="295">6</text>
          <text id="lbl-head"    x="240" y="196">6</text>
        </g>
      </svg>
    </div>

    <!-- PALETTE -->
    <div class="palette-panel">
      <div class="pal-heading">Colour palette</div>
      <div id="palette"></div>
    </div>

  </div>

  <!-- BOTTOM BAR -->
  <div class="bot-bar">
    <div class="prog-row">
      <span class="prog-lbl" id="prog-text">0 of 17 areas coloured</span>
      <span class="prog-pct" id="prog-pct">0%</span>
    </div>
    <div class="prog-track"><div class="prog-fill" id="prog-fill"></div></div>
    <div id="hint-box"><div class="hint idle">Choose a colour number from the palette to begin.</div></div>
  </div>

</main>

<!-- PRINT FOOTER -->
<div id="print-ftr" style="text-align:center;padding:14px;font-size:12px;color:#888;border-top:1px solid #E5E7EB;margin-top:8px;display:none">
  Kivora Creative Kingdom · Educational Colouring Activity · Ages 4–8 · kivora.lk
</div>

<!-- META STRIP -->
<div class="meta-strip">
  <div><div class="meta-lbl">Learning objective</div><div class="meta-val">Colour &amp; number recognition, fine motor skills</div></div>
  <div><div class="meta-lbl">Age group</div><div class="meta-val">4–8 years (Grades K–2)</div></div>
  <div><div class="meta-lbl">Difficulty</div><div class="meta-val">Beginner · 8 colours · 17 areas</div></div>
  <div><div class="meta-lbl">World</div><div class="meta-val">Creative Kingdom</div></div>
</div>

<!-- CONFETTI -->
<div id="confetti-layer"></div>

<!-- TOAST -->
<div id="toast">Saved to your gallery!</div>

<!-- COMPLETION OVERLAY -->
<div id="comp-overlay">
  <div class="comp-card">
    <div class="stars-row">
      <span class="star">&#9733;</span>
      <span class="star">&#9733;</span>
      <span class="star">&#9733;</span>
    </div>
    <div class="comp-title">Wonderful work!</div>
    <p class="comp-sub">You coloured the whole butterfly!<br>You're a Creative Kingdom artist.</p>
    <div class="comp-actions">
      <button class="cbtn c-gallery" id="btn-save">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
        Save to My Gallery
      </button>
      <button class="cbtn c-dl" id="btn-dl">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        Download as PNG
      </button>
      <button class="cbtn c-print" id="btn-print">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
        Print / Save as PDF
      </button>
      <button class="cbtn c-reset" id="btn-again">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-4.5"/></svg>
        Colour again
      </button>
    </div>
  </div>
</div>

<!-- GALLERY MODAL -->
<div id="gal-modal">
  <div class="gal-card">
    <div class="gal-hdr">
      <div class="gal-title">My Gallery</div>
      <button class="close-btn" id="gal-close">&#x2715;</button>
    </div>
    <div class="gal-grid" id="gal-grid"></div>
  </div>
</div>

<!-- VIEWER -->
<div id="viewer">
  <div id="viewer-box"></div>
  <div class="viewer-acts">
    <button class="vbtn dl" id="v-dl">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      Download PNG
    </button>
    <button class="vbtn pr" id="v-pr">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
      Print / PDF
    </button>
    <button class="vbtn cl" id="v-cl">&#x2715; Close</button>
  </div>
</div>

<script>
(function(){
'use strict';

/* ── DATA ── */
const PAL = {
  1:{hex:'#87CEEB',name:'Sky blue'},
  2:{hex:'#FFD700',name:'Sunny gold'},
  3:{hex:'#D9D9D9',name:'Cloud white'},
  4:{hex:'#FF85B3',name:'Petal pink'},
  5:{hex:'#FF9040',name:'Wing orange'},
  6:{hex:'#9575CD',name:'Body purple'},
  7:{hex:'#5BB85A',name:'Leaf green'},
  8:{hex:'#EF5350',name:'Flower red'}
};
const REGIONS=[
  {rid:'sky',num:1},{rid:'sun',num:2},{rid:'fc1',num:2},{rid:'fc2',num:2},{rid:'fc3',num:2},
  {rid:'cloud-l',num:3},{rid:'cloud-r',num:3},{rid:'grass',num:7},
  {rid:'f1p',num:8},{rid:'f2p',num:8},{rid:'f3p',num:8},
  {rid:'wing-ll',num:5},{rid:'wing-lr',num:5},
  {rid:'wing-ul',num:4},{rid:'wing-ur',num:4},
  {rid:'body',num:6},{rid:'head',num:6}
];
const TOTAL=REGIONS.length;
const GKEY='kivora_butterfly_gallery';

/* ── STATE ── */
let activeNum=null, colored={}, viewerSvg=null;

/* ── AUDIO ── */
let AC=null;
const ac=()=>{ if(!AC) AC=new(window.AudioContext||window.webkitAudioContext)(); return AC; };
const tone=(freq,start,dur,vol=0.2)=>{
  try{
    const c=ac(), o=c.createOscillator(), g=c.createGain();
    o.connect(g); g.connect(c.destination);
    o.type='sine'; o.frequency.value=freq;
    g.gain.setValueAtTime(vol,c.currentTime+start);
    g.gain.exponentialRampToValueAtTime(0.001,c.currentTime+start+dur);
    o.start(c.currentTime+start); o.stop(c.currentTime+start+dur);
  }catch(e){}
};
const sndFill=()=>{ tone(440,0,.06,.15); tone(660,0.05,.2,.12); };
const sndNumber=()=>{ [523,659,784,1047].forEach((f,i)=>tone(f,i*.1,.28,.2)); };
const sndCelebrate=()=>{ [523,659,784,659,784,1047,784,1047,1319].forEach((f,i)=>tone(f,i*.11,.22,.18)); };

/* ── SVG HELPERS ── */
const NS='http://www.w3.org/2000/svg';
const rEl=rid=>document.querySelector(`[data-rid="${rid}"]`);
const lEl=rid=>document.getElementById(`lbl-${rid}`);

const drawSunRays=()=>{
  const g=document.getElementById('sun-rays'); g.innerHTML='';
  const c=colored.sun?PAL[2].hex:'#BBBBBB';
  [0,45,90,135,180,225,270,315].forEach(a=>{
    const r=a*Math.PI/180, l=document.createElementNS(NS,'line');
    l.setAttribute('x1',400+53*Math.cos(r)); l.setAttribute('y1',76+53*Math.sin(r));
    l.setAttribute('x2',400+68*Math.cos(r)); l.setAttribute('y2',76+68*Math.sin(r));
    l.setAttribute('stroke',c); l.setAttribute('stroke-width','3'); l.setAttribute('stroke-linecap','round');
    g.appendChild(l);
  });
};

/* ── REGION STYLING ── */
const styleR=r=>{
  const el=rEl(r.rid), lb=lEl(r.rid); if(!el) return;
  const done=!!colored[r.rid], hl=activeNum===r.num&&!done;
  if(done){
    el.setAttribute('fill',PAL[r.num].hex);
    el.setAttribute('stroke','rgba(0,0,0,0.12)'); el.setAttribute('stroke-width','1.5');
    el.classList.remove('hl-r');
    if(lb) lb.style.display='none';
  } else if(hl){
    el.setAttribute('fill','url(#hlPat)');
    el.setAttribute('stroke','#7B9FFF'); el.setAttribute('stroke-width','2.5');
    el.classList.add('hl-r');
    if(lb){ lb.style.display=''; lb.setAttribute('fill','#111'); lb.setAttribute('font-size','14'); }
  } else {
    el.setAttribute('fill','#EFEFEF');
    el.setAttribute('stroke','#BBBBBB'); el.setAttribute('stroke-width','1.5');
    el.classList.remove('hl-r');
    if(lb){ lb.style.display=''; lb.setAttribute('fill','#555'); lb.setAttribute('font-size','12'); }
  }
};

/* ── PROGRESS ── */
const upProg=()=>{
  const n=Object.keys(colored).length, pct=Math.round(n/TOTAL*100);
  document.getElementById('prog-fill').style.width=pct+'%';
  document.getElementById('prog-text').textContent=`${n} of ${TOTAL} areas coloured`;
  document.getElementById('prog-pct').textContent=pct+'%';
};

/* ── HINT ── */
const upHint=()=>{
  const box=document.getElementById('hint-box');
  const n=Object.keys(colored).length;
  if(n===TOTAL){
    box.innerHTML='<div class="hint complete">All areas coloured — your butterfly is complete!</div>';
  } else if(activeNum!==null){
    const rem=REGIONS.filter(r=>r.num===activeNum&&!colored[r.rid]).length;
    const dot=`<span class="hint-dot" style="background:${PAL[activeNum].hex}"></span>`;
    box.innerHTML=`<div class="hint active">${dot}Tap the areas marked <strong>${activeNum}</strong> &ndash; ${rem} area${rem!==1?'s':''} left</div>`;
  } else {
    box.innerHTML='<div class="hint idle">Choose a colour number from the palette to begin.</div>';
  }
};

/* ── STATS ── */
const stats=()=>{
  const m={};
  for(const r of REGIONS){
    if(!m[r.num]) m[r.num]={total:0,filled:0};
    m[r.num].total++;
    if(colored[r.rid]) m[r.num].filled++;
  }
  return m;
};

/* ── PALETTE ── */
const buildPal=()=>{
  const pal=document.getElementById('palette'), st=stats();
  pal.innerHTML='';
  Object.entries(PAL).forEach(([k,{hex,name}])=>{
    const num=+k, s=st[num]||{total:0,filled:0};
    const done=s.filled===s.total, act=activeNum===num;
    const item=document.createElement('div');
    item.className='pal-item'+(done?' pdone':'')+(act?' pactive':'');
    if(act){ item.style.borderColor='#222'; item.style.boxShadow=`0 6px 20px ${hex}90`; }
    const chk=`<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="rgba(0,0,0,0.5)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="4,12 9,17 20,6"/></svg>`;
    item.innerHTML=`
      <div class="pal-color" style="background:${hex}">
        ${done?chk:`<span>${num}</span>`}
      </div>
      <div class="pal-info">
        <span class="pal-name">${name}</span>
        <span class="pal-count${done?' done':''}">${done?'Done!':s.filled+'/'+s.total}</span>
      </div>`;
    if(!done) item.addEventListener('click',()=>{ activeNum=activeNum===num?null:num; renderAll(); });
    pal.appendChild(item);
  });
};

/* ── RENDER ALL ── */
const renderAll=()=>{ REGIONS.forEach(styleR); drawSunRays(); upProg(); buildPal(); upHint(); };

/* ── CANVAS CLICK ── */
document.getElementById('canvas').addEventListener('click',e=>{
  const el=e.target.closest('[data-rid]'); if(!el) return;
  const rid=el.getAttribute('data-rid'), num=+el.getAttribute('data-num');
  if(activeNum===null||num!==activeNum||colored[rid]) return;

  colored[rid]=true;
  /* Flash-fill animation */
  el.style.transition='none'; el.setAttribute('fill','#FFFFFF');
  requestAnimationFrame(()=>requestAnimationFrame(()=>{
    el.style.transition='fill 0.28s ease';
    el.setAttribute('fill',PAL[num].hex);
    el.setAttribute('stroke','rgba(0,0,0,0.12)'); el.setAttribute('stroke-width','1.5');
    el.classList.remove('hl-r');
    const lb=lEl(rid); if(lb) lb.style.display='none';
  }));

  sndFill(); drawSunRays(); upProg();
  const st=stats(); buildPal(); upHint();

  if(st[num].filled===st[num].total){
    sndNumber();
    setTimeout(()=>{
      document.querySelectorAll('.pal-item.pdone').forEach(el=>{
        el.classList.remove('wobble'); void el.offsetWidth; el.classList.add('wobble');
      });
    },80);
  }
  if(Object.keys(colored).length===TOTAL) setTimeout(celebrate,550);
});

/* ── CELEBRATION ── */
const celebrate=()=>{
  sndCelebrate();
  launchConfetti();
  setTimeout(()=>document.getElementById('comp-overlay').classList.add('show'),1300);
};

/* ── CONFETTI ── */
const launchConfetti=()=>{
  const con=document.getElementById('confetti-layer');
  con.innerHTML=''; con.style.display='block';
  const cols=['#FF85B3','#FFD700','#9575CD','#5BB85A','#FF9040','#87CEEB','#EF5350','#4ECDC4','#FFE66D','#FF6B6B'];
  const shapes=['50%','2px','50% 2px','0'];
  for(let i=0;i<90;i++){
    const p=document.createElement('div');
    const sz=Math.random()*10+5, drift=(Math.random()-.5)*240;
    const dur=Math.random()*2+2.2, delay=Math.random()*.9;
    p.className='cp';
    p.style.cssText=`left:${Math.random()*100}%;width:${sz}px;height:${sz*(Math.random()*1.8+.5)}px;background:${cols[i%cols.length]};border-radius:${shapes[Math.floor(Math.random()*shapes.length)]};--drift:${drift}px;animation-duration:${dur}s;animation-delay:${delay}s;`;
    con.appendChild(p);
  }
  setTimeout(()=>{ con.innerHTML=''; con.style.display='none'; },5000);
};

/* ── EXPORT HELPERS ── */
const captureSVG=()=>{
  const sv=document.getElementById('canvas').cloneNode(true);
  sv.querySelectorAll('.hl-r').forEach(e=>e.classList.remove('hl-r'));
  return new XMLSerializer().serializeToString(sv);
};

const svgToPNG=(svgStr,cb)=>{
  const cv=document.createElement('canvas'); cv.width=cv.height=1200;
  const ctx=cv.getContext('2d'); ctx.fillStyle='#fff'; ctx.fillRect(0,0,1200,1200);
  const blob=new Blob([svgStr],{type:'image/svg+xml;charset=utf-8'});
  const url=URL.createObjectURL(blob), img=new Image();
  img.onload=()=>{ ctx.drawImage(img,0,0,1200,1200); URL.revokeObjectURL(url); cb(cv.toDataURL('image/png')); };
  img.onerror=()=>{ URL.revokeObjectURL(url); alert('PNG export failed — try right-clicking the image.'); };
  img.src=url;
};

const dlPNG=(svgStr,fname)=>{
  svgToPNG(svgStr,dataURL=>{ const a=document.createElement('a'); a.download=fname; a.href=dataURL; a.click(); });
};

const printSVG=(svgStr,title)=>{
  const w=window.open('','_blank');
  w.document.write(`<!DOCTYPE html><html><head><title>${title}</title>
    <style>body{margin:24px;font-family:sans-serif}svg{max-width:100%;height:auto;display:block;margin:0 auto}
    .hd{display:flex;justify-content:space-between;align-items:center;border-bottom:2px solid #E5E7EB;padding-bottom:10px;margin-bottom:16px}
    .ft{text-align:center;font-size:11px;color:#888;margin-top:16px;border-top:1px solid #E5E7EB;padding-top:10px}
    button{margin-top:16px;padding:10px 24px;font-size:14px;font-weight:600;cursor:pointer;border:1.5px solid #333;border-radius:8px;background:#fff}
    @media print{button{display:none}}</style></head><body>
    <div class="hd"><div><strong style="font-size:16px">Kivora · Creative Kingdom</strong><div style="font-size:12px;color:#666;margin-top:3px">Colour the magic butterfly &mdash; Ages 4&ndash;8 &mdash; Beginner</div></div><div style="font-size:11px;color:#999">kivora.lk</div></div>
    ${svgStr}
    <div class="ft">Kivora Creative Kingdom &middot; Educational Colouring Activity &middot; Grades K&ndash;2</div>
    <button onclick="window.print()">Print / Save as PDF</button>
    </body></html>`);
  w.document.close(); setTimeout(()=>w.print(),400);
};

/* ── COMPLETION ACTIONS ── */
const showToast=msg=>{
  const t=document.getElementById('toast'); t.textContent=msg; t.classList.add('show');
  setTimeout(()=>t.classList.remove('show'),2800);
};

document.getElementById('btn-save').addEventListener('click',()=>{
  const svgStr=captureSVG();
  const gallery=JSON.parse(localStorage.getItem(GKEY)||'[]');
  const now=new Date();
  gallery.unshift({id:now.getTime(),date:now.toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'}),svgData:svgStr});
  if(gallery.length>12) gallery.pop();
  localStorage.setItem(GKEY,JSON.stringify(gallery));
  upGalCount();
  showToast('Saved to your gallery!');
});

document.getElementById('btn-dl').addEventListener('click',()=>dlPNG(captureSVG(),'kivora-butterfly.png'));
document.getElementById('btn-print').addEventListener('click',()=>printSVG(captureSVG(),'Kivora · Magic Butterfly'));
document.getElementById('btn-again').addEventListener('click',doReset);

/* ── GALLERY COUNT ── */
const upGalCount=()=>{
  const g=JSON.parse(localStorage.getItem(GKEY)||'[]');
  const b=document.getElementById('galleryCount');
  b.textContent=g.length; g.length>0?b.classList.add('show'):b.classList.remove('show');
};

/* ── GALLERY OPEN/CLOSE ── */
document.getElementById('galleryBtn').addEventListener('click',openGallery);
document.getElementById('gal-close').addEventListener('click',()=>document.getElementById('gal-modal').classList.remove('show'));
document.getElementById('gal-modal').addEventListener('click',e=>{ if(e.target===document.getElementById('gal-modal')) document.getElementById('gal-modal').classList.remove('show'); });

function openGallery(){
  renderGallery();
  document.getElementById('gal-modal').classList.add('show');
}

function renderGallery(){
  const grid=document.getElementById('gal-grid');
  grid.innerHTML='';
  const gallery=JSON.parse(localStorage.getItem(GKEY)||'[]');
  if(!gallery.length){
    grid.innerHTML='<div class="gal-empty"><div class="gal-empty-icon">&#129419;</div><div class="gal-empty-t">No saved butterflies yet</div><div class="gal-empty-s">Complete the activity and tap Save to My Gallery!</div></div>';
    return;
  }
  gallery.forEach((item,idx)=>{
    const card=document.createElement('div');
    card.className='gal-item';
    const thumb=document.createElement('div');
    thumb.className='gal-thumb'; thumb.innerHTML=item.svgData;
    const sv=thumb.querySelector('svg'); if(sv){ sv.style.width='100%'; sv.style.height='100%'; }
    thumb.addEventListener('click',()=>openViewer(item.svgData));

    const meta=document.createElement('div');
    meta.className='gal-meta';
    meta.innerHTML=`<span class="gal-date">${item.date}</span><div class="gal-acts">
      <button class="gal-act" title="Download PNG" data-idx="${idx}">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      </button>
      <button class="gal-act del" title="Delete" data-del="${idx}">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14H6L5 6"/></svg>
      </button>
    </div>`;
    meta.querySelector('[data-idx]').addEventListener('click',()=>dlPNG(item.svgData,`kivora-butterfly-${item.id}.png`));
    meta.querySelector('[data-del]').addEventListener('click',()=>{
      const g=JSON.parse(localStorage.getItem(GKEY)||'[]');
      g.splice(idx,1); localStorage.setItem(GKEY,JSON.stringify(g)); upGalCount(); renderGallery();
    });
    card.appendChild(thumb); card.appendChild(meta); grid.appendChild(card);
  });
}

/* ── VIEWER ── */
function openViewer(svgStr){
  viewerSvg=svgStr;
  const box=document.getElementById('viewer-box'); box.innerHTML=svgStr;
  const sv=box.querySelector('svg'); if(sv){ sv.style.width='100%'; sv.style.height='100%'; }
  document.getElementById('viewer').classList.add('show');
}
document.getElementById('v-dl').addEventListener('click',()=>{ if(viewerSvg) dlPNG(viewerSvg,'kivora-butterfly.png'); });
document.getElementById('v-pr').addEventListener('click',()=>{ if(viewerSvg) printSVG(viewerSvg,'Kivora · Magic Butterfly'); });
document.getElementById('v-cl').addEventListener('click',()=>{ document.getElementById('viewer').classList.remove('show'); viewerSvg=null; });
document.getElementById('viewer').addEventListener('click',e=>{ if(e.target===document.getElementById('viewer')){ document.getElementById('viewer').classList.remove('show'); viewerSvg=null; }});

/* ── RESET ── */
function doReset(){
  colored={}; activeNum=null;
  document.getElementById('comp-overlay').classList.remove('show');
  renderAll();
}
document.getElementById('resetBtn').addEventListener('click',doReset);

/* ── INIT ── */
drawSunRays(); renderAll(); upGalCount();

})();
</script>
</body>
</html>
HTMLEOF
echo "Done — $(wc -c < /mnt/user-data/outputs/kivora-creative-kingdom-v3.html) bytes"
